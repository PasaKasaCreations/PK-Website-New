# =====================================================
# Pasakasa Creations - Environment Variables
# =====================================================
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to git!
# =====================================================

# =====================================================
# SUPABASE (REQUIRED)
# =====================================================
# Get these from: Supabase Dashboard > Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ⚠️ DO NOT ADD SERVICE ROLE KEY (not needed for public site)
# Only add when building admin dashboard (backend only)

# =====================================================
# ADMIN API AUTHENTICATION
# =====================================================
# Generate a secure random key for API authentication
# Example: openssl rand -hex 32
ADMIN_API_KEY=your-secure-random-api-key-here

# =====================================================
# SITE CONFIGURATION
# =====================================================
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# =====================================================
# PAYMENT GATEWAY (OPTIONAL - for future course enrollment)
# =====================================================
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
# STRIPE_SECRET_KEY=sk_test_...
# (Uncomment when implementing payments)

# =====================================================
# RATE LIMITING (OPTIONAL - for future implementation)
# =====================================================
# UPSTASH_REDIS_REST_URL=https://...
# UPSTASH_REDIS_REST_TOKEN=...
# (Uncomment when implementing rate limiting)

# =====================================================
# EMAIL SERVICE - SMTP2GO (REQUIRED for contact form notifications)
# =====================================================
# Get your API key from: https://app.smtp2go.com/settings/api_keys
SMTP2GO_API_KEY=api-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# The verified sender email address in your SMTP2GO account
SMTP2GO_SENDER_EMAIL=noreply@yourdomain.com

# Email address to receive contact form notifications
NOTIFICATION_EMAIL=contact@pasakasacreations.com

# =====================================================
# WASABI S3 STORAGE (REQUIRED for image uploads)
# =====================================================
# Get your credentials from: Wasabi Console > Access Keys
# Create a bucket and note the region (e.g., ap-south-1, us-east-1)

# Your Wasabi access key ID
WASABI_ACCESS_KEY_ID=your-wasabi-access-key-id

# Your Wasabi secret access key
WASABI_SECRET_ACCESS_KEY=your-wasabi-secret-access-key

# Wasabi bucket name (must already exist)
WASABI_BUCKET_NAME=your-bucket-name

# Wasabi region where your bucket is located
# Available regions: us-east-1, us-east-2, us-central-1, us-west-1,
# eu-central-1, eu-central-2, eu-west-1, eu-west-2,
# ap-northeast-1, ap-northeast-2, ap-southeast-1, ap-southeast-2, ap-south-1
WASABI_REGION=ap-south-1

# =====================================================
# SECURITY NOTES
# =====================================================
# ✅ ANON key is safe to expose (RLS protects data)
# ❌ NEVER expose service role key
# ❌ NEVER commit .env.local to version control
# ✅ Use different keys for development/production
# ❌ NEVER expose Wasabi secret access key
# =====================================================